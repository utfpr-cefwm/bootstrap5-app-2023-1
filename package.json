{
  "name": "bootstrap5-app",
  "version": "0.0.1",
  "description": "Aplicativo de exemplo utilizando BS5 e JS",
  "keywords": [
    "bootstrap",
    "bootstrap5"
  ],
  "license": "MIT",
  "author": "Rui Pimentel Leite",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html",
    "start:front": "npm run build && npm run dev",
    "start:back": "cd server && npm start",
    "start": "npm-run-all --parallel start:front start:back",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "bootstrap-icons": "^1.10.5",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.9.3"
  },
  "dependencies": {
    "bootstrap": "^5.3.1"
  }
}
